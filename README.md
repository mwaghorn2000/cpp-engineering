# cpp-lab

This repository contains my C++ learning, embedded systems practice, DSP experiments, and low-level engineering exercises.

Structure:
- `src/` — source files
- `include/` — header files
- `tests/` (future)
- `modules/` (future DSP / embedded modules)

Built with CMake + modern C++20.
